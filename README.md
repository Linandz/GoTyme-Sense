# GoTyme Sense™ — Final Interactive Johannesburg Edition

This edition is self-contained: the Johannesburg skyline is embedded directly inside `index.html`.

## Update the live Vercel site
Upload this `index.html` to the root of the existing GitHub repository and commit the change. Vercel will redeploy automatically.
